<%= form_for(@devdirectory) do |f| %>
  <% if @devdirectory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@devdirectory.errors.count, "error") %> prohibited this devdirectory from being saved:</h2>

      <ul>
      <% @devdirectory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.text_field :project_id %>
  </div>
  <div class="field">
    <%= f.label :devdirectory_id %><br>
    <%= f.text_field :devdirectory_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
