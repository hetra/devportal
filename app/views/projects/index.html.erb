<p id="notice"><%= notice %></p>

<% @projects.each do |project| %>
  <h2><%= link_to project.name, project_path(project.id) %> <small><%= project.version %></small></h2>
  <p><%= project.description %></p>
  <p><%= project.name %> uses <%= project.license %>.</p>
<% end %>

<br>

<%= link_to 'New Project', new_project_path %>
